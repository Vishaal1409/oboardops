from strands import tool

@tool
def generate_schedule(start_date: str) -> str:
    """Generates a first-week meeting schedule based on the new hire's start date."""
    # TODO: Shruthika implements this — reference first_week_schedule.md
    return f"Schedule starting {start_date} — coming soon"