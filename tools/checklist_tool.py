from strands import tool

@tool
def generate_checklist(role: str, department: str) -> str:
    """Generates a personalized onboarding checklist based on role and department."""
    # TODO: Arun implements this
    return f"Checklist for {role} in {department} — coming soon"